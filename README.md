This script is for create the biz count of New York Taxi
Get the raw data from the GCP public BigQuery table.
But the data quality is not good. 
so after data clease. the data is not complete.
for the linear regression. just using the one parameter to do the regression.
FARE VS TRIP_DISTANCE